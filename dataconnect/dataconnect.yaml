specVersion: "v1beta"
serviceId: "aplaymanage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aplaymanage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
